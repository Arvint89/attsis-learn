# /deploy

Push main branch — triggers GitHub Pages auto-deploy via pages.yml.

```bash
python scripts/smoke_test.py
git push origin main
```

Confirm live URL: https://arvint89.github.io/attsis-learn
Check GitHub Actions tab for deploy status.
